import 'package:flutter/material.dart';

class ResponsiveWiget extends StatelessWidget {
  const ResponsiveWiget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
